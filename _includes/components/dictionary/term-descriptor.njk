{% if summary or not (descriptor.inline and previousTermInline) %}{% set term = term.newTermFrom(descriptor.term, descriptor.collection) %}<div class="descriptor{% if summary and descriptor.inline %} d-inline{% endif %}">{% if summary %}{% if descriptor.inline and previousTermInline %}, {% endif %}{{ term.label() | safe }}{% else %}{{ term.summary() | safe }}{% endif %}</div>{% endif %}