{% from './term-macros.njk' import termLabel, termDescription %}<div class="content-block compact-content summary {% if nested %}nested-summary {% endif %}d-flex gap-2 my-2 flex-row collection-{{ term.collectionId }} term-{{ term.termId }}">{% if term.meta.icon or term.index.meta.icon %}<div class="my-2">{{ termIcon(term) }}</div>{% endif %}<div>{% if nested %}<h4>{{ termLabel(term, true) }}</h4><div class="nested-content">{{ termDescription(term, createTerm, termList, true, true) }}</div>{% else %}<h3>{{ termLabel(term, true) }}</h3><div>{{ termDescription(term, createTerm, termList, true) }}</div>{% endif %}</div></div>