{% from './term-macros.njk' import termLabel, termDescription %}<div class="content-block compact-content summary {% if nested %}nested-summary {% endif %}collection-{{ term.collectionId }} term-{{ term.termId }}">{% if nested %}<h4>{{ termLabel(term, true) }}</h4><div class="nested-content">{{ termDescription(term, createTerm, termList, true, true) }}</div>{% else %}<h3>{{ termLabel(term, true) }}</h3><div>{{ termDescription(term, createTerm, termList, true) }}</div>{% endif %}</div>